**Technology stack: HTML, CSS, JS, REST API, Vite, Git**

A website with responsive layout for finding and storing physical exercises by category.

My part:
Displaying a block with exercise cards, pagination, filtering by category, implemented URL-based state management: filters (e.g., category, exercise) and pagination are stored in the URL, allowing the page to restore its state on reload and be shareable with others.
